# {{ project_name }}

{{ description }}
{%- if mcp %}

A standard Forge MCP service — streamable-HTTP, stateless, config-via-env.

## Run

```bash
uv sync
uv run {{ service_name }}     # streamable-HTTP on 127.0.0.1:$PORT (default {{ port }})
```

## Configure (env)

| Var | Default | Purpose |
| :-- | :-- | :-- |
| `{{ package_name | upper }}_PORT` | `{{ port }}` | streamable-HTTP listen port |
| `{{ package_name | upper }}_HOST` | `127.0.0.1` | bind host — loopback by default; a network-reachable deploy (e.g. behind the Hades gateway) must set this to `0.0.0.0` explicitly in its own env, not rely on the code default |
{%- else %}

A standard Forge Python package.

## Install

```bash
uv sync
```
{%- endif %}

## Develop

```bash
uv sync --extra dev
uv run ruff check .
uv run mypy src tests
uv run pyright
uv run pytest
```
{%- if mcp %}

## Container

```bash
docker build -t {{ service_name }} .
docker run --rm -e PORT={{ port }} -p {{ port }}:{{ port }} {{ service_name }}
```
{%- endif %}
