# {{ project_name }}

{{ description }}
{%- if mcp %}

A standard Forge MCP service — streamable-HTTP, stateless, config-via-env.

## Run

```bash
uv sync
uv run {{ service_name }}     # streamable-HTTP on 0.0.0.0:$PORT (default {{ port }})
```

## Configure (env)

| Var | Default | Purpose |
| :-- | :-- | :-- |
| `PORT` | `{{ port }}` | streamable-HTTP listen port |
| `HOST` | `0.0.0.0` | bind host |
{%- else %}

A standard Forge Python package.

## Install

```bash
uv sync
```
{%- endif %}

## Develop

```bash
uv sync --extra dev
uv run ruff check .
uv run mypy src tests
uv run pyright
uv run pytest
```
{%- if mcp %}

## Container

```bash
docker build -t {{ service_name }} .
docker run --rm -e PORT={{ port }} -p {{ port }}:{{ port }} {{ service_name }}
```
{%- endif %}
